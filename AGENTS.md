# AGENTS.md

本文件适用于本仓库的所有协作者，包括人类与 AI。若子目录存在更具体的 `AGENTS.md`，应同时遵守其补充规则。

## 分支与合并规则

- `dev` 是日常开发与集成分支；所有更新必须先进入 `dev`。
- 新功能或修复应从最新 `dev` 创建 feature 分支，并通过 Pull Request（或等价的评审流程）合并回 `dev`。
- feature 分支不得直接提交、推送或合并到 `master`。
- `master` 只接受经过 `dev` 集成和验证后的发布变更，发布时应使用从 `dev` 到 `master` 的 Pull Request；禁止绕过 `dev` 直接交付 `master`。
- 未经明确授权，不要改写共享分支历史，不要强制推送。

## 构建门禁

- 任何代码、文档、配置、依赖或其他项目文件的更新，在提交或请求合并到 `dev` 前都必须完成构建验证。
- 本项目的标准构建命令是：

  ```bash
  pixi run --locked build
  ```

  该命令执行 `pixi.toml` 中的 `build` 任务；本规则中所说的“通过 pixi build”指通过上述标准命令。
- 只有命令成功退出，才可以提交变更或创建/更新合并请求。构建失败时必须先修复，不能以绕过构建、直接使用裸 `pnpm` 或忽略错误的方式交付。
- 若构建因环境、依赖下载或其他外部原因无法完成，应在交付说明中如实记录，并在获得明确许可前不要将变更合并到 `dev`。

## 推荐工作流程

1. 开始前确认工作区状态和当前分支，保留与任务无关的现有改动。
2. 从最新 `dev` 创建或更新 feature 分支并完成修改。
3. 运行 `pixi run --locked build`，确认成功后再提交。
4. 将 feature 分支提交 Pull Request 合并到 `dev`，经过集成验证后再由 `dev` 发布到 `master`。
